<%= render partial: "shared/pet_form", locals: {path: "/pets/#{@pet.id}",
                                    method_type: :patch,
                                    button_text: 'Update Pet',
                                    model: @pet,
                                    title: "Edit #{@pet.name}"}%>
